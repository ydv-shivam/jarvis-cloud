JARVIS Cloud v3 diagnostic package.
Cloudflare root: /JARVIS_Cloud_v3/worker
Deploy command: npx wrangler deploy
Required secrets: JARVIS_CLIENT_TOKEN and OPENAI_API_KEY.
The /health endpoint reports only whether each secret exists; it never reveals secret values.
